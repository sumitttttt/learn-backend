<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="px-3 py-2 bg-red-500 rounded-md mt-2" href="/logout">Logout</a>
        </div>

        <h3 class="mb-5" >Edit Your Post</h3>
        <form action="/update/<%=post._id%>" method="post">
            <textarea placeholder="post something here.." class="block p-3 w-1/3 resize-none bg-transparent border-2 border-zinc-600 rounded-md" name="content"><%=post.content%></textarea>
            <input class="px-3 py-2 bg-yellow-500 block rounded-md mt-2" type="Submit" value="Update Post">
        </form>
        
    </div>
</body>
</html>